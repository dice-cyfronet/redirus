#!/usr/bin/env ruby
require 'rubygems'
require 'sidekiq'
require_relative '../lib/redirus/worker'

RUNNER_PATH = File.join(ROOT_PATH, 'lib', 'redirus', 'runner.rb')

system "sidekiq -q #{Redirus::Worker.config.queue} -c 1 -r #{RUNNER_PATH}"